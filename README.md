# rexbrahh Homebrew Tap

Homebrew tap for Rex Liu's macOS apps and command-line tools.

## Usage

```sh
brew tap rexbrahh/tap
brew install --cask icloud-guard
```

## Contents

- `Casks/` contains GUI app casks.
- `Formula/` contains command-line formulae.

